pluginManagement {
  repositories {
    mavenLocal()
    maven {
      name "ave"
      url aveMavenUrl
      credentials {
        username aveMavenUser
        password aveMavenPassword
      }
    }
    gradlePluginPortal()
  }
}

rootProject.name = "vim-subversive"
